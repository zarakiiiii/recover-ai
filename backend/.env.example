# Backend Application Settings
PROJECT_NAME=RecoverAI
ENVIRONMENT=development
PORT=8000

# PostgreSQL Settings
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=recoverai
POSTGRES_PASSWORD=recoverai
POSTGRES_DB=recoverai_db

# Optional direct DATABASE_URL override (defaults to postgresql+psycopg://...)
# DATABASE_URL=postgresql+psycopg://recoverai:recoverai@localhost:5432/recoverai_db
